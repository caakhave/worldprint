# Homepage hero media

`can-you-geo-hero.jpg` is the current full-bleed homepage hero background.
It is a temporary conversion of the existing premium atlas artwork.

When the final generated cinematic asset is ready, replace this with either:
- `can-you-geo-hero.webp`
- `can-you-geo-hero.mp4`

Then update the `.landing-hero::before` media path in `src/styles/globals.css`.
